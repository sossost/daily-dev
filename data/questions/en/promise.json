[
  {
    "id": "promise-001",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "What does Promise.race() return?",
    "options": [
      "The result of the first settled (fulfilled or rejected) Promise",
      "Only the result of the first fulfilled Promise",
      "Waits for all Promises to complete, then returns the fastest one",
      "The result of the Promise with the smallest index in the array"
    ],
    "correctIndex": 0,
    "explanation": "Promise.race() fulfills or rejects with the result of the first settled (fulfilled or rejected) Promise. It returns the first completed result regardless of whether it was fulfilled or rejected. This is the key difference from Promise.any(), which only returns the first fulfillment.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/race"
  },
  {
    "id": "promise-002",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "easy",
    "question": "What is the output of the following code?",
    "code": "Promise.resolve(1)\n  .then(v => v + 1)\n  .then(v => v * 3)\n  .then(v => console.log(v));",
    "options": [
      "1",
      "6",
      "3",
      "NaN"
    ],
    "correctIndex": 1,
    "explanation": "In Promise chaining, each then callback's return value is passed to the next then. Promise.resolve(1) -> then(v => v + 1) returns 2 -> then(v => v * 3) returns 6 -> then(v => console.log(v)) outputs 6. The value explicitly returned from a then callback becomes the fulfillment value of the next Promise.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then"
  },
  {
    "id": "promise-003",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output of the following code?",
    "code": "Promise.resolve('A')\n  .then(v => {\n    console.log(v);\n    return 'B';\n  })\n  .then(v => {\n    console.log(v);\n  })\n  .then(v => {\n    console.log(v);\n  });",
    "options": [
      "A, A, A",
      "A, B, B",
      "A, B, undefined",
      "A, undefined, undefined"
    ],
    "correctIndex": 2,
    "explanation": "The first then outputs 'A' and returns 'B'. The second then outputs 'B' but has no explicit return, so it implicitly returns undefined. The third then outputs the undefined received from the previous then. When a then callback has no return statement, undefined is implicitly returned and becomes the argument for the next then.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then"
  },
  {
    "id": "promise-004",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "Which statement about the error handling difference between Promise.all() and Promise.race() is correct?",
    "options": [
      "Promise.all ignores errors and Promise.race propagates them",
      "Both wait for all Promises to complete before handling errors",
      "Promise.all collects all errors into an array",
      "Promise.all rejects immediately on the first rejection, Promise.race returns the result of the first settled Promise"
    ],
    "correctIndex": 3,
    "explanation": "Promise.all() rejects immediately with the error when any Promise rejects (fail-fast). Promise.race() returns the result of the first settled Promise (fulfilled or rejected) — if the fastest one rejects, the race rejects; if it fulfills, the race fulfills. The semantic difference is: Promise.all means 'all must succeed to succeed', Promise.race means 'the fastest one decides'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/all"
  },
  {
    "id": "promise-005",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "What is a 'thenable' object in JavaScript?",
    "options": [
      "An object with a then method that provides a Promise-compatible interface",
      "A special term that only refers to Promise instances",
      "Any object that has a catch method",
      "An async iterable object that implements Symbol.iterator"
    ],
    "correctIndex": 0,
    "explanation": "A thenable is any object that has a then method. When a thenable is passed to Promise.resolve(), it calls the object's then method to convert it into a Promise. Thanks to this protocol, async objects from non-Promise libraries can be integrated into Promise chains. The ECMAScript spec also refers to this as 'PromiseLike'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise#thenables"
  },
  {
    "id": "promise-006",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "Which statement about the key difference between Promise.all() and Promise.allSettled() is correct?",
    "options": [
      "Promise.all rejects immediately if any Promise rejects, while Promise.allSettled waits for all Promises to complete and returns each result",
      "Promise.allSettled automatically retries rejected Promises",
      "Promise.all returns results as { status, value } object arrays, Promise.allSettled returns value arrays",
      "Both work identically, but Promise.allSettled is a newer API with better performance"
    ],
    "correctIndex": 0,
    "explanation": "Promise.all() returns a result array when all Promises fulfill, but rejects immediately if any one rejects (fail-fast). Promise.allSettled() waits for all Promises to settle regardless of fulfillment or rejection, returning each result as { status: 'fulfilled', value } or { status: 'rejected', reason }. allSettled is suitable for parallel operations where partial failures are acceptable.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/allSettled"
  },
  {
    "id": "promise-007",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "const p = new Promise((resolve) => {\n  resolve(1);\n  resolve(2);\n  console.log(3);\n});\n\np.then(v => console.log(v));",
    "options": [
      "3, 2",
      "1, 3",
      "3, 1",
      "3, 1, 2"
    ],
    "correctIndex": 2,
    "explanation": "The Promise executor function executes synchronously. resolve(1) is called but the then callback is registered as a microtask and does not execute immediately. resolve(2) is ignored — once a Promise is settled, its state cannot change (immutable). console.log(3) executes synchronously, outputting 3 first. Then the microtask then callback executes, outputting 1.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise"
  },
  {
    "id": "promise-008",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "medium",
    "question": "Why is the error not properly handled in the following code?",
    "code": "function fetchData() {\n  return fetch('/api/data')\n    .then(res => res.json());\n}\n\ntry {\n  fetchData();\n} catch (err) {\n  console.log('caught:', err);\n}",
    "options": [
      "The fetch function does not throw errors",
      "res.json() always succeeds",
      "The catch block syntax is incorrect",
      "try/catch only catches synchronous code; asynchronous Promise rejections are not caught"
    ],
    "correctIndex": 3,
    "explanation": "try/catch only captures synchronously thrown exceptions. fetchData() returns a Promise, and network errors or JSON parsing errors occur asynchronously as Promise rejections. To handle them, chain .catch() or use async/await with try/catch. This pattern is a common mistake and a major cause of UnhandledPromiseRejection warnings.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#error_handling"
  },
  {
    "id": "promise-009",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "Promise.all([\n  Promise.resolve(1),\n  Promise.reject('error'),\n  Promise.resolve(3)\n])\n  .then(v => console.log('then:', v))\n  .catch(e => console.log('catch:', e));",
    "options": [
      "catch: error",
      "then: [1, 'error', 3]",
      "then: [1, undefined, 3]",
      "catch: ['error']"
    ],
    "correctIndex": 0,
    "explanation": "Promise.all() rejects immediately when any Promise rejects (fail-fast behavior). The second Promise rejects with 'error', so Promise.all does not wait for the rest and immediately rejects with 'error'. The catch handler receives the rejection reason 'error' and outputs 'catch: error'. To get all results regardless, use Promise.allSettled().",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/all"
  },
  {
    "id": "promise-010",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "What error is thrown when all Promises passed to Promise.any() are rejected?",
    "options": [
      "TypeError",
      "AggregateError",
      "RangeError",
      "RejectionError"
    ],
    "correctIndex": 1,
    "explanation": "Promise.any() returns the value of the first fulfillment, but if all Promises reject, it throws an AggregateError. AggregateError, introduced in ES2021, contains an errors property with an array of all rejection reasons, allowing you to inspect each individual failure.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/any"
  },
  {
    "id": "promise-011",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "medium",
    "question": "What causes the unintended behavior in the following code?",
    "code": "async function processItems(items) {\n  items.forEach(async (item) => {\n    await saveItem(item);\n  });\n  console.log('All items saved!');\n}",
    "options": [
      "forEach throws an error when receiving a non-array value",
      "forEach does not await the async callback's returned Promise, so the log outputs before all saves complete",
      "Using async inside forEach causes a syntax error",
      "await does not work inside forEach callbacks so saveItem never executes"
    ],
    "correctIndex": 1,
    "explanation": "Array.prototype.forEach() ignores the callback's return value. The async callback returns a Promise, but forEach does not await it, so the next line executes before all saveItem calls complete. The correct approach is to use a for...of loop with await, or Promise.all(items.map(async (item) => saveItem(item))).",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/forEach"
  },
  {
    "id": "promise-012",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "console.log('1');\n\nPromise.resolve().then(() => {\n  console.log('2');\n  Promise.resolve().then(() => console.log('3'));\n});\n\nPromise.resolve().then(() => console.log('4'));\n\nconsole.log('5');",
    "options": [
      "1, 5, 2, 3, 4",
      "1, 5, 2, 4, 3",
      "1, 2, 4, 5, 3",
      "1, 5, 4, 2, 3"
    ],
    "correctIndex": 1,
    "explanation": "Synchronous code executes first: 1, 5. The microtask queue has two then callbacks registered in order. The first microtask executes, outputting 2, and registers a new microtask (output 3) in the queue. The second microtask executes, outputting 4. Then the internally registered microtask executes, outputting 3. Final order: 1, 5, 2, 4, 3. Microtasks are processed in FIFO order.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API/Microtask_guide"
  },
  {
    "id": "promise-013",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "What is the difference between .catch(onRejected) and .then(undefined, onRejected)?",
    "options": [
      "They behave identically, but .catch can also catch errors from the preceding then, which is the practical difference in positioning",
      ".catch catches synchronous errors too but .then's second argument only catches async errors",
      ".catch collects multiple errors into an array",
      ".then's second argument is deprecated and can no longer be used"
    ],
    "correctIndex": 0,
    "explanation": ".catch(fn) is shorthand for .then(undefined, fn) and is identical per the spec. The practical difference is in chain positioning. In p.then(onFulfilled, onRejected), onRejected cannot catch errors thrown in the same then's onFulfilled. But in p.then(onFulfilled).catch(onRejected), catch can catch errors from then's onFulfilled. This is why .catch() at the end of the chain is the idiomatic approach.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/catch"
  },
  {
    "id": "promise-014",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "What is the shape of each element in the result array of Promise.allSettled()?",
    "options": [
      "{ resolved: boolean, data: any }",
      "{ status: 'fulfilled', value } or { status: 'rejected', reason }",
      "The fulfilled value or an Error object",
      "{ success: boolean, result: any }"
    ],
    "correctIndex": 1,
    "explanation": "Promise.allSettled() waits for all Promises to settle and returns each result in a standard format. Fulfilled cases are { status: 'fulfilled', value }, and rejected cases are { status: 'rejected', reason }. The status field serves as a discriminated union, which can be leveraged for type narrowing in TypeScript.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/allSettled"
  },
  {
    "id": "promise-015",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "Promise.reject('fail')\n  .catch(e => {\n    console.log('caught:', e);\n    return 'recovered';\n  })\n  .then(v => console.log('then:', v))\n  .catch(e => console.log('catch2:', e));",
    "options": [
      "caught: fail, catch2: recovered",
      "caught: fail",
      "caught: fail, then: recovered",
      "catch2: fail"
    ],
    "correctIndex": 2,
    "explanation": "When a catch handler returns a value, the error is 'recovered'. The first catch handles reject('fail'), outputs 'caught: fail', and returns 'recovered'. The return value from catch becomes a fulfilled Promise, so the next then receives 'recovered' and outputs 'then: recovered'. The second catch does not execute. This pattern is called error recovery.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#error_handling"
  },
  {
    "id": "promise-016",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "medium",
    "question": "What causes the unintended behavior in the following code?",
    "code": "function getUser(id) {\n  return fetch(`/api/users/${id}`)\n    .then(res => {\n      if (!res.ok) {\n        throw new Error('Not found');\n      }\n      res.json(); // 의도: 파싱된 JSON 반환\n    });\n}",
    "options": [
      "fetch works synchronously",
      "The res.ok check is incorrect",
      "throw new Error is used without a catch",
      "res.json() is missing return, so the next then receives undefined"
    ],
    "correctIndex": 3,
    "explanation": "Inside the then callback, res.json() returns a Promise, but without the return keyword, this value is discarded. The callback implicitly returns undefined, and the next then receives undefined. The correct code is 'return res.json();'. This mistake often occurs when confusing arrow function implicit returns (without { }) with explicit returns.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#common_mistakes"
  },
  {
    "id": "promise-017",
    "topic": "promise",
    "type": "concept",
    "difficulty": "hard",
    "question": "How are Promise then callbacks scheduled in the event loop?",
    "options": [
      "Registered in the microtask queue, executing after the current execution context completes but before the next macrotask",
      "Registered in the macrotask queue, executing with the same priority as setTimeout callbacks",
      "Executed immediately and synchronously when the call stack is empty",
      "Executed separately in a Web Worker thread"
    ],
    "correctIndex": 0,
    "explanation": "Promise then/catch/finally callbacks are registered in the microtask queue. After the currently executing task completes, all microtask queue items are processed before the next macrotask (setTimeout, I/O, etc.). This is why Promise callbacks always execute before setTimeout(fn, 0). This is defined by the ECMAScript spec's HostEnqueuePromiseJob.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API/Microtask_guide"
  },
  {
    "id": "promise-018",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "Which statement about the behavior of Promise.prototype.finally() is correct?",
    "options": [
      "The finally callback receives the fulfillment value or rejection reason as an argument",
      "finally only executes when the Promise fulfills, not when it rejects",
      "The finally callback's return value becomes the next chain's value",
      "The finally callback is called with no arguments and passes through the previous Promise's value"
    ],
    "correctIndex": 3,
    "explanation": "The finally() callback receives no arguments and always executes whether the Promise fulfills or rejects. The callback's return value is ignored, and the previous Promise's fulfillment value or rejection reason passes through to the next chain as-is. However, if the finally callback throws an error or returns a rejected Promise, the chain rejects with that error. It is suitable for cleanup logic.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/finally"
  },
  {
    "id": "promise-019",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "Which statement about the error handling difference between async/await and Promise.then() chaining is correct?",
    "options": [
      "async/await automatically ignores errors while then chaining propagates them",
      "then chaining errors cannot be caught with try/catch",
      "async/await handles errors with try/catch like synchronous code, then chaining uses .catch() method",
      "async/await terminates the process on error while then chaining continues execution"
    ],
    "correctIndex": 2,
    "explanation": "async/await allows handling asynchronous errors with try/catch blocks just like synchronous code. With then chaining, errors are handled using .catch() method or the second argument of .then(). Both internally deal with Promise rejections, but async/await provides a more intuitive control flow that helps prevent missed error handling.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Learn_web_development/Extensions/Async_JS/Promises"
  },
  {
    "id": "promise-020",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "hard",
    "question": "What is the anti-pattern in the following code?",
    "code": "function readData(url) {\n  return new Promise((resolve, reject) => {\n    fetch(url)\n      .then(res => res.json())\n      .then(data => resolve(data))\n      .catch(err => reject(err));\n  });\n}",
    "options": [
      "fetch does not return a Promise, so wrapping with new Promise is needed",
      "Calling reject in catch causes an infinite loop",
      "res.json() is synchronous so then is unnecessary",
      "fetch already returns a Promise, so the unnecessary Promise wrapping is the explicit construction anti-pattern"
    ],
    "correctIndex": 3,
    "explanation": "fetch() already returns a Promise, so wrapping it with new Promise is unnecessary. This is known as the 'explicit construction anti-pattern' or 'Promise constructor anti-pattern'. The correct code is simply return fetch(url).then(res => res.json()). Unnecessary wrapping makes code more complex, increases the risk of missed error handling, and creates unnecessarily deep stack traces during debugging.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#common_mistakes"
  },
  {
    "id": "promise-021",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output of the following code?",
    "code": "Promise.resolve('hello')\n  .then(v => {\n    return new Promise(resolve => {\n      setTimeout(() => resolve(v + ' world'), 100);\n    });\n  })\n  .then(v => console.log(v));",
    "options": [
      "hello world",
      "hello",
      "undefined",
      "[object Promise]"
    ],
    "correctIndex": 0,
    "explanation": "When a then callback returns a Promise, the next then waits for that Promise to settle. The first then returns a new Promise that resolves with 'hello world' after 100ms. The second then receives this resolved value and outputs it. When a Promise is returned from then, the chain automatically 'flattens' it — the final resolved value is passed, not a nested Promise.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then"
  },
  {
    "id": "promise-022",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "When does the window's 'unhandledrejection' event fire?",
    "options": [
      "When all Promises are fulfilled",
      "When a Promise is rejected but no .catch() or rejection handler is attached",
      "When an error occurs inside a try/catch block",
      "Only when Promise.all() fails"
    ],
    "correctIndex": 1,
    "explanation": "The unhandledrejection event fires when a Promise is rejected but no rejection handler (.catch() or then's second argument) is attached. The event's event.reason property provides access to the rejection reason, and calling event.preventDefault() suppresses the console error. In Node.js, process.on('unhandledRejection') detects the same situation.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Window/unhandledrejection_event"
  },
  {
    "id": "promise-023",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "Which statement about the difference between Promise.any() and Promise.race() is correct?",
    "options": [
      "Promise.any() only accepts async iterables while Promise.race() only accepts arrays",
      "Promise.race() ignores rejections and only returns fulfilled results",
      "Both return the first fulfilled Promise but differ only in error handling",
      "Promise.any() returns the first fulfilled Promise, while Promise.race() returns the result of the first settled Promise whether fulfilled or rejected"
    ],
    "correctIndex": 3,
    "explanation": "Promise.any() returns the result of the first fulfilled Promise, ignoring rejected ones. If all Promises reject, it throws an AggregateError. Promise.race() returns the result of the first settled Promise regardless of whether it fulfilled or rejected. In other words, race returns the fastest 'result', while any returns the fastest 'success'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/any"
  },
  {
    "id": "promise-024",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "medium",
    "question": "What causes the unintended behavior in the following code?",
    "code": "async function getData() {\n  const result = await fetch('/api/data');\n  return result;\n}\n\nconst data = getData();\nconsole.log(data.status);",
    "options": [
      "fetch does not return a Response object to result",
      "async functions always return a Promise, so calling without await means data is a Promise object without a status property",
      "await fails to unwrap the fetch Promise",
      "Return values from async functions cannot be used outside"
    ],
    "correctIndex": 1,
    "explanation": "async functions always return a Promise. Calling getData() without await assigns a Promise object to the data variable. The Promise object does not have an HTTP status property, so data.status is undefined. The correct approach is const data = await getData(), or getData().then(data => console.log(data.status)).",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function"
  },
  {
    "id": "promise-025",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "Promise.resolve(1)\n  .then(v => { throw new Error('oops'); })\n  .then(v => console.log('A:', v))\n  .catch(e => {\n    console.log('B:', e.message);\n    return 42;\n  })\n  .then(v => console.log('C:', v));",
    "options": [
      "A: 1, C: 42",
      "B: oops",
      "B: oops, C: 42",
      "B: oops, A: undefined, C: 42"
    ],
    "correctIndex": 2,
    "explanation": "Throwing an error in the first then rejects that Promise. The second then's fulfillment handler (A output) is skipped, and catch catches the error, outputting 'B: oops'. Returning 42 from catch recovers the error into a fulfilled Promise, so the final then outputs 'C: 42'. In Promise chains, errors propagate to the nearest rejection handler.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#chaining_after_a_catch"
  },
  {
    "id": "promise-026",
    "topic": "promise",
    "type": "concept",
    "difficulty": "medium",
    "question": "What happens when a thenable object is passed to Promise.resolve()?",
    "code": "const thenable = {\n  then(resolve) {\n    resolve(42);\n  }\n};\nPromise.resolve(thenable).then(v => console.log(v));",
    "options": [
      "The thenable object itself is passed as the argument to then",
      "A TypeError is thrown",
      "42 is output",
      "undefined is output"
    ],
    "correctIndex": 2,
    "explanation": "When Promise.resolve() receives a thenable (an object with a then method), it calls the object's then method to convert it into a Promise. Since the thenable's then calls resolve(42), the resulting Promise fulfills with 42. This behavior is used to make third-party library Promise-like objects compatible with native Promises.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/resolve"
  },
  {
    "id": "promise-027",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "hard",
    "question": "What is the problem in the following code?",
    "code": "const promises = urls.map(url => fetch(url));\n\nfor (const p of promises) {\n  const res = await p;\n  const data = await res.json();\n  process(data);\n}",
    "options": [
      "fetch cannot be called inside a map callback",
      "await cannot be used in a for...of loop, causing a syntax error",
      "All fetches start concurrently but results are processed sequentially, so if one fails, remaining Promise rejections may go unhandled",
      "The promises array is not iterable so for...of does not work"
    ],
    "correctIndex": 2,
    "explanation": "urls.map(url => fetch(url)) starts all fetches concurrently. Sequentially awaiting in for...of means if one Promise rejects and throws an error, the remaining already-started fetch Promises' rejections go unhandled, potentially causing unhandled rejection errors. The safe approach is to use Promise.allSettled(promises) to handle all results at once, or attach .catch() to each Promise beforehand.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#error_handling"
  },
  {
    "id": "promise-028",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "easy",
    "question": "What is the difference between Promise.resolve(value) and new Promise(resolve => resolve(value))?",
    "options": [
      "The two expressions are completely identical with no differences",
      "Promise.resolve returns the value as-is if it is already a Promise, while new Promise always creates a new Promise",
      "new Promise executes faster",
      "Promise.resolve returns the value synchronously while new Promise returns it asynchronously"
    ],
    "correctIndex": 1,
    "explanation": "Promise.resolve() returns the argument as-is if it is already a Promise (specifically, a Promise from the same constructor). new Promise(resolve => resolve(value)) always creates a new Promise object. When the value is a plain non-Promise value, both expressions behave identically. This difference matters for avoiding unnecessary Promise wrapping and optimizing performance.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/resolve"
  },
  {
    "id": "promise-029",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output of the following code?",
    "code": "Promise.all([]).then(v => console.log(v));",
    "options": [
      "undefined",
      "[]",
      "Promise { <pending> }",
      "A TypeError is thrown"
    ],
    "correctIndex": 1,
    "explanation": "Passing an empty array to Promise.all() returns an immediately fulfilled Promise with an empty array ([]) as the value. This is the identity element behavior for empty input — since there are no Promises to reject, it immediately succeeds. In contrast, Promise.race([]) with an empty array stays pending forever.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/all"
  },
  {
    "id": "promise-030",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "What happens when an error is synchronously thrown inside a Promise executor function?",
    "options": [
      "The Promise is rejected with that error",
      "A global error occurs and the program terminates",
      "The error is ignored and the Promise stays pending",
      "The executor function is re-executed"
    ],
    "correctIndex": 0,
    "explanation": "When an error is thrown inside new Promise((resolve, reject) => { ... }), the Promise constructor automatically catches it and rejects the Promise with that error. This has the same effect as explicitly calling reject(error). Thanks to this behavior, errors inside the executor are safely converted to Promise rejections without needing try/catch.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise"
  },
  {
    "id": "promise-031",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "async function foo() {\n  return 'hello';\n}\n\nconst result = foo();\nconsole.log(result instanceof Promise);\nconsole.log(typeof result);",
    "options": [
      "false, string",
      "true, object",
      "true, string",
      "false, object"
    ],
    "correctIndex": 1,
    "explanation": "async functions always return a Promise. Even though 'hello' is returned, it is automatically wrapped as Promise.resolve('hello'). Therefore result is a Promise instance — instanceof Promise is true, and typeof is 'object'. To access the async function's return value, you must use await or .then().",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function"
  },
  {
    "id": "promise-032",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "What is the difference between sequential await in a for...of loop and parallel execution with Promise.all()?",
    "options": [
      "Sequential await completes each task before starting the next, while Promise.all starts all tasks simultaneously for parallel execution",
      "Promise.all runs sequentially but processes faster",
      "Sequential await is always faster than Promise.all",
      "Both approaches have identical execution order and performance"
    ],
    "correctIndex": 0,
    "explanation": "Using await in a for...of loop means each async operation completes before the next one starts (sequential execution). Promise.all(items.map(fn)) starts all async operations immediately and waits for all to complete (parallel execution). For independent tasks, parallel processing with Promise.all significantly reduces total elapsed time. Use sequential execution only when order matters or there are dependencies between tasks.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for-await...of"
  },
  {
    "id": "promise-033",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "medium",
    "question": "What is the problem in the following code?",
    "code": "function timeout(ms) {\n  return new Promise((_, reject) => {\n    setTimeout(() => reject(new Error('Timeout')), ms);\n  });\n}\n\nasync function fetchWithTimeout(url) {\n  const result = await Promise.race([\n    fetch(url),\n    timeout(5000)\n  ]);\n  return result.json();\n}",
    "options": [
      "Promise.race cannot accept an array as argument",
      "Even if fetch succeeds, the timeout timer keeps running and may cause an unhandled rejection later",
      "result.json() does not execute synchronously",
      "The timeout function should use resolve instead of reject"
    ],
    "correctIndex": 1,
    "explanation": "Promise.race() returns the result of the first settled Promise but does not cancel the remaining Promises. If fetch succeeds within 5 seconds, the result is returned, but the timeout's setTimeout continues running and will reject after 5 seconds. With no handler for this rejection, an unhandled rejection occurs. Use AbortController to cancel the fetch, or attach .catch() to the timeout Promise to solve this.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/AbortController"
  },
  {
    "id": "promise-034",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "const p = Promise.resolve('A');\n\np.then(v => console.log(v + '1'));\np.then(v => console.log(v + '2'));\np.then(v => console.log(v + '3'));",
    "options": [
      "A1",
      "A3",
      "A1, A2, A3",
      "A1, A3, A2"
    ],
    "correctIndex": 2,
    "explanation": "Attaching multiple then handlers to the same Promise creates branching, not chaining. Each then independently receives the same fulfillment value 'A'. They execute in the order registered in the microtask queue: A1, A2, A3. This differs from chaining (then().then().then()), where each handler receives the previous handler's return value instead of the original Promise's value.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises#chaining"
  },
  {
    "id": "promise-035",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "What happens when an empty array is passed to Promise.race()?",
    "options": [
      "It immediately fulfills with undefined",
      "A TypeError is thrown",
      "It immediately fulfills with an empty array",
      "It stays pending forever"
    ],
    "correctIndex": 3,
    "explanation": "Promise.race([]) with an empty array returns a Promise that stays pending forever because there are no Promises to settle. This contrasts with Promise.all([]), which immediately fulfills with an empty array. Since race returns the 'fastest result' and there are no Promises to compete, the result is never determined. This behavior can be an unintentional source of memory leaks.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/race"
  },
  {
    "id": "promise-036",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "hard",
    "question": "What causes the unintended behavior in the following code?",
    "code": "async function retryFetch(url, retries = 3) {\n  for (let i = 0; i < retries; i++) {\n    const res = await fetch(url).catch(() => null);\n    if (res) return res.json();\n  }\n  throw new Error('All retries failed');\n}",
    "options": [
      "await cannot be used in a for loop",
      "catch(() => null) does not handle errors correctly",
      "Even when res is truthy, HTTP error statuses (4xx, 5xx) are not checked, so failed responses are treated as successes",
      "res.json() does not return a Promise"
    ],
    "correctIndex": 2,
    "explanation": "fetch() only rejects on network errors. HTTP 4xx and 5xx responses do not cause rejection — they return a normal Response object. So even when res is not null (no network error), it could still be a server error response. You must check res.ok or res.status to handle HTTP errors. This is one of the most common mistakes with the fetch API.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch#checking_that_the_fetch_was_successful"
  },
  {
    "id": "promise-037",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "What is the difference between returning a value and returning a Promise from a then callback?",
    "options": [
      "Returning a value is processed synchronously, returning a Promise is processed asynchronously",
      "Returning a value wraps it with Promise.resolve() and passes it to the next then, returning a Promise makes the chain wait until it settles",
      "Returning a Promise passes the nested Promise as-is",
      "Returning a value or a Promise behaves identically"
    ],
    "correctIndex": 1,
    "explanation": "When a then callback returns a plain value, it is wrapped with Promise.resolve(value) and passed to the next then immediately (in the next microtask). When a Promise is returned, the chain waits until that Promise fulfills or rejects, then passes the fulfilled value to the next then. Thanks to this automatic flattening, returning a Promise does not create nesting, enabling clean chaining.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then"
  },
  {
    "id": "promise-038",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "Can a Promise's state be changed after it has been settled (fulfilled or rejected)?",
    "options": [
      "Calling resolve again changes it to a new value",
      "Calling reject can change a fulfilled state to rejected",
      "A Promise's state cannot be changed once settled (immutable)",
      "The state can be changed within a then handler"
    ],
    "correctIndex": 2,
    "explanation": "A Promise's state transitions only once from pending to either fulfilled or rejected. Any subsequent calls to resolve or reject are ignored. This immutability is a core design principle of Promises, ensuring that async operation results are predictable and stable. This guarantees that multiple then handlers always receive the same value.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise"
  },
  {
    "id": "promise-039",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "When does the Promise.prototype.finally() callback execute?",
    "options": [
      "Only when the Promise is fulfilled",
      "Only when the Promise is rejected",
      "Only after catch handles an error",
      "Always, regardless of whether the Promise is fulfilled or rejected"
    ],
    "correctIndex": 3,
    "explanation": "The finally() callback executes regardless of the Promise's outcome (fulfilled or rejected). finally() receives no arguments and passes through the previous Promise's value unchanged. It is ideal for cleanup logic such as releasing resources or hiding loading indicators that should run regardless of the result.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/finally"
  },
  {
    "id": "promise-040",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "console.log('1');\nnew Promise((resolve) => {\n  console.log('2');\n  resolve();\n}).then(() => {\n  console.log('3');\n});\nconsole.log('4');",
    "options": [
      "1, 2, 4, 3",
      "1, 2, 3, 4",
      "1, 4, 2, 3",
      "1, 3, 2, 4"
    ],
    "correctIndex": 0,
    "explanation": "The Promise constructor's executor function runs synchronously. So after '1' is logged, '2' inside the executor is logged immediately. resolve() is called, but the then callback is queued as a microtask and will only execute after all synchronous code finishes. '4' is logged next, and finally '3' is logged when the microtask runs.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise"
  },
  {
    "id": "promise-041",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "hard",
    "question": "Why does the second then always receive undefined in this code?",
    "code": "fetch('/api/data')\n  .then(res => {\n    res.json();\n  })\n  .then(data => {\n    console.log(data); // undefined\n  });",
    "options": [
      "Because fetch does not automatically parse the response body",
      "Because the first then does not return the result of res.json(), so undefined is implicitly returned",
      "Because res.json() is a synchronous function and cannot be used in a then chain",
      "Because the second then has no error handler"
    ],
    "correctIndex": 1,
    "explanation": "When using curly braces {} in a then callback, an explicit return statement is required. res.json() returns a Promise, but since the callback does not return it, the callback itself returns undefined. Fix it by adding 'return res.json()' or using the arrow function's implicit return (res => res.json()). This is one of the most common mistakes in Promise chaining.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then"
  },
  {
    "id": "promise-042",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "medium",
    "question": "What is the key difference between Promise.allSettled() and Promise.all()?",
    "options": [
      "allSettled() does not guarantee order, but all() does",
      "all() runs in parallel while allSettled() runs sequentially",
      "allSettled() waits for all Promises to settle and does not short-circuit when one rejects",
      "allSettled() returns only fulfilled results while all() returns all results"
    ],
    "correctIndex": 2,
    "explanation": "Promise.all() rejects immediately (short-circuits) when any Promise rejects. In contrast, Promise.allSettled() waits until all Promises have settled (fulfilled or rejected) and returns an array of {status, value} or {status, reason} objects for each result. allSettled() is ideal when you need all results even if some fail.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/allSettled"
  },
  {
    "id": "promise-043",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "Promise.resolve()\n  .then(() => console.log('A'))\n  .then(() => console.log('B'));\n\nPromise.resolve()\n  .then(() => console.log('C'))\n  .then(() => console.log('D'));",
    "options": [
      "A, B, C, D",
      "C, D, A, B",
      "A, C, D, B",
      "A, C, B, D"
    ],
    "correctIndex": 3,
    "explanation": "The microtask queue processes tasks in FIFO order. First, two Promise.resolve().then() calls each register a microtask: logging 'A' and logging 'C'. 'A' runs first and registers 'B' in the queue. Then 'C' runs and registers 'D' in the queue. After that, 'B' and 'D' run in order. Chained then callbacks are only queued after the previous then completes.",
    "sourceUrl": "https://javascript.info/microtask-queue"
  },
  {
    "id": "promise-044",
    "topic": "promise",
    "type": "concept",
    "difficulty": "medium",
    "question": "What is a thenable object?",
    "options": [
      "Any object with a then method, which Promise methods treat as a Promise",
      "Only objects that inherit from Promise.prototype",
      "A special object returned by async functions",
      "An object that implements the Symbol.thenable symbol"
    ],
    "correctIndex": 0,
    "explanation": "A thenable is any object that has a then(onFulfilled, onRejected) method. Promise.resolve(), await, and then() chains check if a returned object is thenable, and if so, call its then method to delegate resolution. This enables interoperability with various Promise libraries (Q, Bluebird, etc.).",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise#thenables"
  },
  {
    "id": "promise-045",
    "topic": "promise",
    "type": "debugging",
    "difficulty": "easy",
    "question": "Why is the error not caught in this code?",
    "code": "Promise.reject('error')\n  .then(val => console.log(val))\n  .then(val => console.log(val));",
    "options": [
      "Because a string was passed to reject instead of an Error object",
      "Because there is no catch or onRejected handler in the chain, so the rejection goes unhandled",
      "Because calling then twice in a row makes the error disappear",
      "Because Promise.reject() throws an error synchronously"
    ],
    "correctIndex": 1,
    "explanation": "When a Promise rejects, the rejection propagates down the chain until it finds the nearest rejection handler (catch or a second argument to then). This code has neither a catch nor an onRejected handler, so the rejection goes unhandled, resulting in an UnhandledPromiseRejection. Add .catch(err => ...) or .then(null, err => ...) to handle it.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/catch"
  },
  {
    "id": "promise-046",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "const thenable = {\n  then(resolve) {\n    resolve(42);\n  }\n};\n\nPromise.resolve(thenable).then(val => console.log(val));",
    "options": [
      "{ then: [Function: then] }",
      "undefined",
      "Promise { 42 }",
      "42"
    ],
    "correctIndex": 3,
    "explanation": "When a thenable object (an object with a then method) is passed to Promise.resolve(), it calls the object's then method and delegates resolution. The thenable's then calls resolve(42), so the Promise fulfills with 42. The next then callback logs 42. This is Promise's automatic thenable unwrapping mechanism.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/resolve#resolving_thenables"
  },
  {
    "id": "promise-047",
    "topic": "promise",
    "type": "comparison",
    "difficulty": "easy",
    "question": "What is the difference between new Promise(executor) and Promise.resolve(value)?",
    "options": [
      "new Promise takes an executor function to start async work, while Promise.resolve creates an immediately fulfilled Promise with the given value",
      "Both behave identically and differ only in syntax",
      "Promise.resolve internally calls new Promise, so it is slower",
      "new Promise is always asynchronous while Promise.resolve returns a value synchronously"
    ],
    "correctIndex": 0,
    "explanation": "new Promise(executor) takes an executor function where you manually call resolve/reject, making it suitable for wrapping async operations. Promise.resolve(value) is a convenience method that creates an immediately fulfilled Promise with the given value. However, if a thenable is passed, it calls the thenable's then method and awaits the result. In both cases, then callbacks run asynchronously as microtasks.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/resolve"
  },
  {
    "id": "promise-048",
    "topic": "promise",
    "type": "concept",
    "difficulty": "medium",
    "question": "Which event should you use to detect unhandled Promise rejections in the browser?",
    "options": [
      "window.onerror event",
      "error event",
      "unhandledrejection event",
      "promiseerror event"
    ],
    "correctIndex": 2,
    "explanation": "In browsers, the 'unhandledrejection' event detects unhandled Promise rejections. Register it with window.addEventListener('unhandledrejection', handler). The event object provides promise and reason properties to access the rejected Promise and its reason. window.onerror only catches synchronous errors and does not detect Promise rejections.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Window/unhandledrejection_event"
  },
  {
    "id": "promise-049",
    "topic": "promise",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output of the following code?",
    "code": "const p = Promise.resolve();\n\np.then(() => {\n  console.log('A');\n  return Promise.resolve('B');\n}).then((val) => {\n  console.log(val);\n});\n\np.then(() => {\n  console.log('C');\n}).then(() => {\n  console.log('D');\n});",
    "options": [
      "A, C, B, D",
      "A, B, C, D",
      "A, C, D, B",
      "C, A, D, B"
    ],
    "correctIndex": 2,
    "explanation": "Both chains start from the same resolved Promise p. First microtask tick: 'A' and 'C' log (both are first-level .then handlers). Returning Promise.resolve('B') from the first chain introduces extra microtask ticks for thenable unwrapping (the spec requires at least one additional tick). So the second chain's 'D' runs before 'B' resolves. Final order: A, C, D, B.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then"
  },
  {
    "id": "promise-050",
    "topic": "promise",
    "type": "concept",
    "difficulty": "easy",
    "question": "What happens if you call resolve() multiple times inside a Promise constructor?",
    "code": "const p = new Promise((resolve) => {\n  resolve('first');\n  resolve('second');\n  console.log('inside');\n});\np.then((val) => console.log(val));",
    "options": [
      "An error is thrown because resolve was called twice",
      "The Promise resolves with 'second' (last call wins)",
      "'inside' logs, then the Promise resolves with 'first' — subsequent resolve calls are silently ignored",
      "The Promise resolves with both values as an array ['first', 'second']"
    ],
    "correctIndex": 2,
    "explanation": "A Promise can only be settled (resolved or rejected) once. The first call to resolve('first') sets the Promise's state to fulfilled with value 'first'. The second resolve('second') is silently ignored — it does not throw an error or change the value. The executor continues running synchronously, so 'inside' logs. Then the .then callback runs with 'first'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise"
  }
]
