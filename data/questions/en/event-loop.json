[
  {
    "id": "event-loop-001",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "easy",
    "question": "What best describes the role of the JavaScript event loop?",
    "options": [
      "It moves callbacks from the task queue to the call stack when the call stack is empty",
      "It is a multi-threaded system for executing code in parallel",
      "It is a dedicated engine that only handles DOM events",
      "It is a garbage collector that manages memory"
    ],
    "correctIndex": 0,
    "explanation": "The event loop is a runtime mechanism that continuously monitors the call stack and task queues (macrotask and microtask). When the call stack is empty, it first processes all microtask queue entries, then moves one callback from the macrotask queue to the call stack for execution. JavaScript is single-threaded, but the event loop enables asynchronous processing \u2014 this model is called the 'concurrency model'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-002",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "easy",
    "question": "What is the output order of the following code?",
    "code": "console.log('1');\nsetTimeout(() => console.log('2'), 0);\nconsole.log('3');",
    "options": [
      "1, 2, 3",
      "1, 3, 2",
      "2, 1, 3",
      "3, 1, 2"
    ],
    "correctIndex": 1,
    "explanation": "Synchronous code always completes first. console.log('1') executes, then setTimeout(fn, 0) registers the callback in the macrotask queue asynchronously even with a 0ms delay (the minimum delay varies by browser, but 0ms still executes after synchronous code). console.log('3') completes the synchronous code, then the callback executes from the task queue to output '2'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setTimeout"
  },
  {
    "id": "event-loop-003",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "console.log('start');\nsetTimeout(() => console.log('timeout'), 0);\nPromise.resolve().then(() => console.log('promise'));\nconsole.log('end');",
    "options": [
      "start, end, timeout, promise",
      "start, end, promise, timeout",
      "start, promise, end, timeout",
      "start, timeout, promise, end"
    ],
    "correctIndex": 1,
    "explanation": "Microtasks (Promise) have higher priority than macrotasks (setTimeout). Synchronous code 'start' and 'end' execute first. When the call stack empties, 'promise' executes from the microtask queue, followed by 'timeout' from the macrotask queue.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-004",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "medium",
    "question": "What is the difference between microtasks and macrotasks?",
    "options": [
      "Macrotasks have higher priority than microtasks",
      "Microtasks execute synchronously",
      "Their execution order is random",
      "Microtasks execute immediately after the current task completes, before any macrotask"
    ],
    "correctIndex": 3,
    "explanation": "Microtasks (Promise, queueMicrotask, MutationObserver) are processed immediately after the current task completes, before the next macrotask. Macrotasks (setTimeout, setInterval, I/O) only execute after the microtask queue is empty.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-005",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "setTimeout(() => console.log('A'), 0);\nPromise.resolve()\n  .then(() => {\n    console.log('B');\n    return Promise.resolve();\n  })\n  .then(() => console.log('C'));\nPromise.resolve().then(() => console.log('D'));",
    "options": [
      "B, D, C, A",
      "A, B, D, C",
      "B, C, D, A",
      "D, B, C, A"
    ],
    "correctIndex": 0,
    "explanation": "Microtasks execute before macrotasks. 'B' from the first then and 'D' from the second Promise are registered in the microtask queue. 'B' executes first, then 'D'. The then for 'C' (from the Promise.resolve() returned in B) executes next, and finally macrotask 'A' runs.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-006",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "async function foo() {\n  console.log('foo start');\n  await Promise.resolve();\n  console.log('foo end');\n}\nconsole.log('script start');\nfoo();\nconsole.log('script end');",
    "options": [
      "script start, foo start, foo end, script end",
      "script start, foo start, script end, foo end",
      "foo start, script start, script end, foo end",
      "script start, script end, foo start, foo end"
    ],
    "correctIndex": 1,
    "explanation": "Synchronous code 'script start' outputs first. Calling foo() synchronously outputs 'foo start'. The code after await ('foo end') is registered as a microtask. Then synchronous code 'script end' outputs. When the call stack empties, the microtask 'foo end' executes.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function"
  },
  {
    "id": "event-loop-007",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "easy",
    "question": "Which statement about the Call Stack is correct?",
    "options": [
      "A structure that can execute multiple functions simultaneously",
      "A queue that stores asynchronous callbacks",
      "A LIFO (Last In, First Out) structure that tracks currently executing functions",
      "A dedicated area for processing DOM events"
    ],
    "correctIndex": 2,
    "explanation": "The call stack is a LIFO (Last In, First Out) structure that manages currently executing execution contexts. When a function is called it is pushed onto the stack, and when it returns it is popped. Since JavaScript is single-threaded, there is only one call stack, and only one function executes at a time.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Glossary/Call_stack"
  },
  {
    "id": "event-loop-008",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "easy",
    "question": "What is the output order of the following code?",
    "code": "console.log('A');\nsetTimeout(() => console.log('B'), 0);\nsetTimeout(() => console.log('C'), 0);\nconsole.log('D');",
    "options": [
      "A, B, C, D",
      "A, D, C, B",
      "A, B, D, C",
      "A, D, B, C"
    ],
    "correctIndex": 3,
    "explanation": "Synchronous code 'A' and 'D' execute first. Both setTimeout callbacks are registered in the macrotask queue. When the call stack empties, they execute in registration order: 'B', then 'C'. The macrotask queue processes in FIFO (First In, First Out) order.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setTimeout"
  },
  {
    "id": "event-loop-009",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "easy",
    "question": "Why might the setTimeout in the following code not execute exactly after 1 second?",
    "code": "setTimeout(() => console.log('executed'), 1000);\nfor (let i = 0; i < 1000000000; i++) {}",
    "options": [
      "The synchronous for loop occupies the call stack, so the setTimeout callback must wait",
      "setTimeout is an unreliable API that does not guarantee exact timing",
      "The for loop cancels the setTimeout",
      "setTimeout and the for loop execute simultaneously"
    ],
    "correctIndex": 0,
    "explanation": "The setTimeout delay is a 'minimum wait time'. After 1 second, the callback is added to the task queue, but it can only execute when the call stack is empty. If a heavy synchronous operation (the for loop) is occupying the call stack, the callback waits until that operation finishes.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setTimeout"
  },
  {
    "id": "event-loop-010",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "easy",
    "question": "Which statement about the relationship between the Task Queue and Call Stack is correct?",
    "options": [
      "Task queue callbacks execute immediately even when the call stack is not empty",
      "The call stack and task queue process work simultaneously",
      "The event loop only moves callbacks from the task queue to the call stack when the call stack is empty",
      "The task queue always has higher priority than the call stack"
    ],
    "correctIndex": 2,
    "explanation": "The event loop continuously checks whether the call stack is completely empty. Only when it is empty does it move waiting callbacks from the task queue to the call stack one at a time. While synchronous code is executing, asynchronous callbacks can never interrupt.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-011",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "Promise.resolve().then(() => console.log('A'));\nsetTimeout(() => console.log('B'), 0);\nPromise.resolve().then(() => console.log('C'));\nconsole.log('D');",
    "options": [
      "D, B, A, C",
      "D, A, B, C",
      "A, C, D, B",
      "D, A, C, B"
    ],
    "correctIndex": 3,
    "explanation": "First, synchronous code 'D' executes. Then the Promise callbacks in the microtask queue execute in order: 'A', 'C'. Once the microtask queue is empty, the setTimeout callback from the macrotask queue executes: 'B'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-012",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "medium",
    "question": "Why does the UI freeze after clicking the button in the following code?",
    "code": "button.onclick = function() {\n  for (let i = 0; i < 10000000; i++) {\n    processData(i);\n  }\n  updateUI();\n};",
    "options": [
      "JavaScript is single-threaded, so the synchronous loop blocks UI rendering while occupying the call stack",
      "The onclick event executes asynchronously",
      "The processData function uses excessive memory",
      "updateUI executes before the for loop"
    ],
    "correctIndex": 0,
    "explanation": "JavaScript and UI rendering run on the same main thread. When a heavy synchronous loop occupies the call stack, the event loop cannot process rendering updates, causing the UI to freeze. Solutions: delegate to a Web Worker, or break the work into small chunks and schedule them with requestAnimationFrame or setTimeout.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-013",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "medium",
    "question": "What is the correct purpose of queueMicrotask()?",
    "options": [
      "Adds a task to the macrotask queue",
      "Executes a function synchronously and immediately",
      "Adds a task to the microtask queue to execute immediately after the current task completes",
      "Delegates work to a Web Worker"
    ],
    "correctIndex": 2,
    "explanation": "queueMicrotask() adds a callback to the microtask queue. Microtasks execute immediately after the current task completes, before the next macrotask. It has the same priority as Promise.then() and is useful when consistent asynchronous behavior is needed.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-014",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "medium",
    "question": "Which statement about the difference between requestAnimationFrame and setTimeout is correct?",
    "options": [
      "requestAnimationFrame executes synchronously",
      "setTimeout is more suitable for animations",
      "Both are registered in the same task queue",
      "requestAnimationFrame executes in sync with the browser's rendering cycle, making it suitable for smooth animations"
    ],
    "correctIndex": 3,
    "explanation": "requestAnimationFrame executes its callback just before the browser's next repaint (usually 60fps = ~16.7ms). setTimeout registers in the macrotask queue after the specified time, independent of the refresh rate. requestAnimationFrame is better for animations and automatically pauses when the tab is inactive.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/window/requestAnimationFrame"
  },
  {
    "id": "event-loop-015",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "console.log('1');\nqueueMicrotask(() => console.log('2'));\nsetTimeout(() => console.log('3'), 0);\nqueueMicrotask(() => console.log('4'));\nconsole.log('5');",
    "options": [
      "1, 5, 2, 4, 3",
      "1, 2, 4, 5, 3",
      "1, 5, 3, 2, 4",
      "1, 5, 2, 3, 4"
    ],
    "correctIndex": 0,
    "explanation": "Synchronous code '1' and '5' execute first. Then the microtask queue processes in registration order: '2', '4'. Finally, the setTimeout macrotask callback executes: '3'. queueMicrotask and Promise.then use the same microtask queue.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-016",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "medium",
    "question": "In the following code, the while loop runs infinitely. What is the cause?",
    "code": "let done = false;\nsetTimeout(() => { done = true; }, 1000);\nwhile (!done) {\n  // waiting...\n}",
    "options": [
      "done is declared with const so it cannot be changed",
      "The synchronous while loop occupies the call stack, so the setTimeout callback never gets a chance to execute",
      "The setTimeout delay is too long",
      "Variables declared with let cannot be accessed inside setTimeout"
    ],
    "correctIndex": 1,
    "explanation": "The while loop is synchronous code that continuously occupies the call stack. The setTimeout callback waits in the macrotask queue, but the call stack must be empty for it to execute. Since the while loop never ends, the call stack never empties and done stays false forever. Busy-waiting should never be used in JavaScript.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-017",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "async function async1() {\n  console.log('async1 start');\n  await async2();\n  console.log('async1 end');\n}\nasync function async2() {\n  console.log('async2');\n}\nconsole.log('script start');\nsetTimeout(() => console.log('setTimeout'), 0);\nasync1();\nconsole.log('script end');",
    "options": [
      "script start, async1 start, async2, async1 end, script end, setTimeout",
      "script start, async1 start, async2, script end, setTimeout, async1 end",
      "script start, async1 start, async2, script end, async1 end, setTimeout",
      "script start, script end, async1 start, async2, async1 end, setTimeout"
    ],
    "correctIndex": 2,
    "explanation": "'script start' outputs synchronously. Calling async1() outputs 'async1 start' synchronously, then calling async2() outputs 'async2'. The code after await becomes a microtask. 'script end' outputs synchronously, then microtask 'async1 end' executes, and finally macrotask 'setTimeout' runs.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function"
  },
  {
    "id": "event-loop-018",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "hard",
    "question": "What is the correct execution order of MutationObserver callbacks, Promise.then callbacks, and setTimeout callbacks?",
    "options": [
      "setTimeout -> Promise.then -> MutationObserver",
      "All execute simultaneously",
      "setTimeout -> MutationObserver -> Promise.then",
      "Promise.then and MutationObserver execute first as microtasks, setTimeout executes later as a macrotask"
    ],
    "correctIndex": 3,
    "explanation": "Both Promise.then and MutationObserver callbacks are classified as microtasks. setTimeout callbacks are macrotasks. Since microtasks are processed before macrotasks, Promise.then and MutationObserver execute first, and setTimeout runs last.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/MutationObserver"
  },
  {
    "id": "event-loop-019",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "hard",
    "question": "In the following code, 'Done' is output before all processing completes. What is the cause?",
    "code": "function processAll(items) {\n  items.forEach(item => {\n    setTimeout(() => {\n      heavyComputation(item);\n    }, 0);\n  });\n}\nprocessAll(largeArray);\nconsole.log('Done');",
    "options": [
      "forEach executes asynchronously",
      "largeArray is empty",
      "setTimeout callbacks are registered in the macrotask queue and execute only after all synchronous code completes",
      "heavyComputation throws an error"
    ],
    "correctIndex": 2,
    "explanation": "forEach itself is synchronous, but the setTimeout inside registers callbacks in the macrotask queue. After forEach registers all setTimeouts, console.log('Done') executes synchronously. The heavyComputation callbacks in the macrotask queue execute sequentially only after the call stack empties.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-020",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "hard",
    "question": "When does 'starvation' occur in the event loop?",
    "options": [
      "When the call stack is empty",
      "When microtasks keep generating new microtasks, preventing macrotasks from executing",
      "When macrotasks execute before microtasks",
      "When the event loop stops automatically"
    ],
    "correctIndex": 1,
    "explanation": "The event loop drains the entire microtask queue before executing any macrotask. If microtasks continuously generate new microtasks, the microtask queue never empties, and macrotasks (setTimeout, I/O, etc.) are starved and never execute. UI rendering is also blocked.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-021",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "setTimeout(() => console.log('A'), 0);\nsetTimeout(() => {\n  console.log('B');\n  Promise.resolve().then(() => console.log('C'));\n}, 0);\nsetTimeout(() => console.log('D'), 0);",
    "options": [
      "A, B, C, D",
      "A, B, D, C",
      "A, D, B, C",
      "B, C, A, D"
    ],
    "correctIndex": 0,
    "explanation": "The three setTimeout callbacks enter the macrotask queue in registration order. 'A' executes first, then 'B'. During B's execution, the Promise.then callback ('C') is registered in the microtask queue. The event loop drains the microtask queue after each macrotask, so 'C' executes before the next macrotask 'D'.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-022",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "easy",
    "question": "Which statement about setInterval is correct?",
    "options": [
      "If the callback takes longer than the interval, callbacks overlap and run simultaneously",
      "Callbacks are registered in the macrotask queue and only execute when the call stack is empty",
      "setInterval is classified as a microtask",
      "It stops automatically without clearInterval"
    ],
    "correctIndex": 1,
    "explanation": "setInterval registers callbacks in the macrotask queue at specified intervals. Since JavaScript is single-threaded, callbacks never run simultaneously \u2014 they only execute when the call stack is empty. If a callback takes longer than the interval, subsequent callbacks queue up but never overlap. You must explicitly stop it with clearInterval.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setInterval"
  },
  {
    "id": "event-loop-023",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "medium",
    "question": "What is the difference between setTimeout(fn, 0) and Promise.resolve().then(fn)?",
    "options": [
      "Promise.then executes first as a microtask, while setTimeout executes later as a macrotask after microtasks are drained",
      "Both are registered in the macrotask queue",
      "setTimeout always executes before Promise",
      "Both behave identically"
    ],
    "correctIndex": 0,
    "explanation": "Promise.resolve().then(fn) registers the callback in the microtask queue, while setTimeout(fn, 0) registers it in the macrotask queue. The event loop drains the entire microtask queue before processing any macrotask when the call stack is empty. So even when registered at the same time, the Promise.then callback always executes before the setTimeout callback.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-024",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "console.log('1');\nsetTimeout(() => {\n  console.log('2');\n  Promise.resolve().then(() => console.log('3'));\n}, 0);\nPromise.resolve().then(() => {\n  console.log('4');\n  setTimeout(() => console.log('5'), 0);\n});\nconsole.log('6');",
    "options": [
      "1, 6, 4, 2, 3, 5",
      "1, 6, 4, 2, 5, 3",
      "1, 6, 2, 4, 3, 5",
      "1, 4, 6, 2, 3, 5"
    ],
    "correctIndex": 0,
    "explanation": "Synchronous code '1' and '6' execute first. When the call stack empties, microtask '4' executes, which registers setTimeout for '5' in the macrotask queue. The microtask queue is now empty, so the first macrotask '2' executes. The microtask '3' created during '2' runs before the next macrotask, and finally '5' executes.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-025",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "medium",
    "question": "Why might 'tick' be output one extra time even after clearInterval is called in the following code?",
    "code": "const id = setInterval(() => {\n  console.log('tick');\n}, 100);\nsetTimeout(() => clearInterval(id), 250);",
    "options": [
      "clearInterval executes asynchronously",
      "If the setInterval callback is already in the macrotask queue, clearInterval stops future registrations but may not cancel already-queued callbacks",
      "setInterval cannot be stopped with clearInterval",
      "The setTimeout delay is imprecise so clearInterval never executes"
    ],
    "correctIndex": 1,
    "explanation": "setInterval registers callbacks in the macrotask queue at specified intervals. When clearInterval is called at 250ms, it stops future callback registrations, but a callback already queued (e.g., the one registered at ~200ms) may still execute. Depending on timing, the third 'tick' may have already been queued, causing one extra output.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setInterval"
  },
  {
    "id": "event-loop-026",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "hard",
    "question": "When does rendering (repaint) occur in the browser event loop?",
    "options": [
      "Only once after all macrotasks complete",
      "Before microtasks",
      "After each macrotask and microtask processing, rendering occurs if needed",
      "Rendering runs in a separate process independent of the event loop"
    ],
    "correctIndex": 2,
    "explanation": "One cycle of the browser event loop is: 'execute 1 macrotask \u2192 drain microtask queue \u2192 render if needed'. Rendering happens when the browser determines a screen update is necessary (typically at 60fps). This is why heavy synchronous work blocks the UI, and why requestAnimationFrame runs just before the render step.",
    "sourceUrl": "https://html.spec.whatwg.org/multipage/webappapis.html#event-loop-processing-model"
  },
  {
    "id": "event-loop-027",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "easy",
    "question": "What is the output order of the following code?",
    "code": "const p = new Promise((resolve) => {\n  console.log('A');\n  resolve();\n  console.log('B');\n});\np.then(() => console.log('C'));\nconsole.log('D');",
    "options": [
      "A, D, B, C",
      "A, B, D, C",
      "A, B, C, D",
      "D, A, B, C"
    ],
    "correctIndex": 1,
    "explanation": "The Promise constructor's executor function runs synchronously. So 'A' is output, resolve() is called, and the remaining executor code continues to output 'B'. The then callback ('C') is registered in the microtask queue. Synchronous code 'D' executes next, then microtask 'C' runs.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise"
  },
  {
    "id": "event-loop-028",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "medium",
    "question": "Which statement about the relationship between Web Workers and the event loop is correct?",
    "options": [
      "Web Workers share the main thread's event loop",
      "Web Workers can directly access the DOM",
      "Web Workers have their own event loop and communicate with the main thread via message passing",
      "Web Worker tasks are classified as microtasks"
    ],
    "correctIndex": 2,
    "explanation": "Web Workers run in separate threads with their own event loops. Communication between the main thread and Workers happens via message passing through postMessage/onmessage. Workers cannot access the DOM. Offloading heavy computation to Workers keeps the main thread's event loop unblocked, maintaining smooth UI.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API"
  },
  {
    "id": "event-loop-029",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "easy",
    "question": "What does the 'run-to-completion' principle mean in JavaScript?",
    "options": [
      "Once a function starts executing, no other code can interrupt it until it completes",
      "All asynchronous operations must complete simultaneously",
      "The event loop runs without ever stopping",
      "Multiple functions can execute on the call stack simultaneously"
    ],
    "correctIndex": 0,
    "explanation": "JavaScript's run-to-completion principle means that once a task starts executing, it cannot be interrupted by another task until it finishes. When a function is running on the call stack, the event loop does not process other callbacks until that function returns. This eliminates shared-state concurrency issues but means long synchronous operations can block the entire thread.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop"
  },
  {
    "id": "event-loop-030",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "setTimeout(() => console.log('A'), 10);\nsetTimeout(() => console.log('B'), 0);\nPromise.resolve()\n  .then(() => console.log('C'))\n  .then(() => console.log('D'));",
    "options": [
      "C, D, A, B",
      "B, C, D, A",
      "C, D, B, A",
      "B, A, C, D"
    ],
    "correctIndex": 2,
    "explanation": "Microtasks execute before macrotasks. The Promise.then callback 'C' runs first as a microtask, followed by the chained 'D' also as a microtask. Then from the macrotask queue, 'B' (0ms delay) executes before 'A' (10ms delay). setTimeout's delay represents the minimum time before the callback is queued, so shorter delays are queued first.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setTimeout"
  },
  {
    "id": "event-loop-031",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "medium",
    "question": "What is the output of the following code?",
    "code": "function recursive() {\n  return recursive();\n}\ntry {\n  recursive();\n} catch (e) {\n  console.log('caught');\n}\nconsole.log('end');",
    "options": [
      "try-catch cannot catch infinite recursion",
      "The recursive function returns undefined",
      "A stack overflow (Maximum call stack size exceeded) occurs, but it is caught by catch, so both 'caught' and 'end' are printed",
      "The event loop automatically stops the recursion"
    ],
    "correctIndex": 2,
    "explanation": "Actually, both 'caught' and 'end' are printed. Infinite recursion exceeds the call stack's maximum size and throws a RangeError (Maximum call stack size exceeded). The try-catch catches this error, printing 'caught', and then the synchronous code 'end' executes normally. A stack overflow does not stop the event loop itself \u2014 if the error is properly handled, execution continues.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Errors/Too_much_recursion"
  },
  {
    "id": "event-loop-032",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "hard",
    "question": "What is the difference between Node.js's process.nextTick() and the browser's queueMicrotask()?",
    "options": [
      "Both are registered in the macrotask queue",
      "process.nextTick uses a separate nextTick queue processed before the microtask queue, while queueMicrotask uses the standard microtask queue",
      "queueMicrotask always executes before process.nextTick",
      "process.nextTick executes synchronously, while queueMicrotask executes asynchronously"
    ],
    "correctIndex": 1,
    "explanation": "In Node.js, process.nextTick() callbacks are placed in a separate nextTick queue that is processed after the current operation completes but before the microtask (Promise.then) queue. So process.nextTick callbacks execute before Promise.then callbacks. queueMicrotask() is a W3C standard API that registers in the same microtask queue as Promise.then. In Node.js, the priority order is: nextTick > microtask > macrotask.",
    "sourceUrl": "https://nodejs.org/en/learn/asynchronous-work/understanding-processnexttick"
  },
  {
    "id": "event-loop-033",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "Promise.resolve().then(() => {\n  console.log('A');\n  queueMicrotask(() => console.log('B'));\n});\nqueueMicrotask(() => {\n  console.log('C');\n  Promise.resolve().then(() => console.log('D'));\n});",
    "options": [
      "A, C, B, D",
      "A, B, C, D",
      "C, A, D, B",
      "A, C, D, B"
    ],
    "correctIndex": 0,
    "explanation": "Both callbacks are microtasks. They execute in registration order: first the Promise.then outputs 'A' and queues 'B' as a microtask. Then the queueMicrotask callback outputs 'C' and queues 'D' as a microtask. Since the microtask queue keeps draining, 'B' and 'D' execute in that order.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-034",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "medium",
    "question": "Which statement about setTimeout's minimum delay in browsers is correct?",
    "options": [
      "setTimeout(fn, 0) always executes exactly after 0ms",
      "When nested setTimeout calls exceed 5 levels, browsers enforce a minimum 4ms delay",
      "The minimum delay is always 1ms across all browsers",
      "setTimeout's delay is included in microtask processing time"
    ],
    "correctIndex": 1,
    "explanation": "According to the HTML spec, when setTimeout/setInterval is nested beyond 5 levels, browsers enforce a minimum 4ms delay (clamping). This prevents nested timers from consuming excessive CPU. So recursively calling setTimeout(fn, 0) runs quickly for the first 4 calls, but subsequent calls are spaced at least 4ms apart.",
    "sourceUrl": "https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#timers"
  },
  {
    "id": "event-loop-035",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "easy",
    "question": "Why does the following code unexpectedly log 5 for every iteration?",
    "code": "for (var i = 0; i < 5; i++) {\n  setTimeout(() => console.log(i), 100);\n}",
    "options": [
      "setTimeout ignores the callback",
      "var has function scope, so after the loop completes i is 5, and all callbacks reference the same i",
      "setTimeout's delay is too short and fires before the loop",
      "console.log executes asynchronously"
    ],
    "correctIndex": 1,
    "explanation": "Variables declared with var have function scope, not block scope. All five setTimeout callbacks share a closure over the same i variable. By the time the callbacks execute, the for loop has completed and i is 5. Solution: use let instead of var, which creates a new binding for each iteration, producing 0, 1, 2, 3, 4.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures"
  },
  {
    "id": "event-loop-036",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "medium",
    "question": "What is the difference between MessageChannel and setTimeout(fn, 0)?",
    "options": [
      "MessageChannel messages are classified as microtasks",
      "setTimeout(fn, 0) is always faster than MessageChannel",
      "MessageChannel's onmessage is a macrotask but is not subject to setTimeout's 4ms minimum delay clamping, enabling faster macrotask scheduling",
      "Both execute with identical priority"
    ],
    "correctIndex": 2,
    "explanation": "MessageChannel's onmessage event is classified as a macrotask, but unlike setTimeout, it is not subject to the 4ms minimum delay clamping for nested calls. This is why React's Scheduler and other libraries use MessageChannel instead of setTimeout(fn, 0) for more precise macrotask scheduling.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/MessageChannel"
  },
  {
    "id": "event-loop-037",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "easy",
    "question": "What does the term 'task' mean in the context of the event loop?",
    "options": [
      "It refers only to Promise callbacks",
      "It refers only to synchronous code running on the call stack",
      "It refers only to work executed in Web Workers",
      "A unit of work processed by the event loop, including script execution, event callbacks, and setTimeout callbacks"
    ],
    "correctIndex": 3,
    "explanation": "In the HTML spec, a task is a structured unit of work that the event loop processes. This includes initial script execution, dispatching DOM event callbacks, and executing setTimeout/setInterval callbacks. The event loop processes one task at a time, drains the microtask queue after each task, and performs rendering if needed.",
    "sourceUrl": "https://html.spec.whatwg.org/multipage/webappapis.html#concept-task"
  },
  {
    "id": "event-loop-038",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "async function first() {\n  console.log('1');\n  await second();\n  console.log('2');\n}\nasync function second() {\n  console.log('3');\n  await Promise.resolve();\n  console.log('4');\n}\nfirst();\nconsole.log('5');",
    "options": [
      "1, 3, 5, 4, 2",
      "1, 3, 4, 5, 2",
      "1, 3, 5, 2, 4",
      "1, 5, 3, 4, 2"
    ],
    "correctIndex": 0,
    "explanation": "Calling first() synchronously outputs '1'. Then await second() calls second(), which synchronously outputs '3'. Inside second(), await Promise.resolve() registers the rest of second() ('4') as a microtask. Control returns to first(), but since second() hasn't completed, first()'s await also suspends. Synchronous code '5' executes. Then microtask '4' runs completing second(), which resolves first()'s await, and '2' executes.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function"
  },
  {
    "id": "event-loop-039",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "easy",
    "question": "What is the correct role of requestIdleCallback?",
    "options": [
      "It registers a callback in the microtask queue",
      "It executes a callback synchronously and immediately",
      "It delegates work to a Web Worker",
      "It executes a callback when the browser is idle, allowing low-priority work without blocking the main thread"
    ],
    "correctIndex": 3,
    "explanation": "requestIdleCallback executes callbacks when the browser's main thread is idle. In an event loop cycle, after macrotasks, microtasks, and rendering are all processed, any remaining time (idle period) is used to run these callbacks. This is ideal for low-priority tasks like sending analytics data or background computation, ensuring they don't affect user interactions or animations.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Window/requestIdleCallback"
  },
  {
    "id": "event-loop-040",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "console.log('A');\nPromise.resolve()\n  .then(() => console.log('B'))\n  .then(() => console.log('C'));\nPromise.resolve()\n  .then(() => console.log('D'))\n  .then(() => console.log('E'));\nconsole.log('F');",
    "options": [
      "A, F, B, C, D, E",
      "A, F, B, D, C, E",
      "A, F, D, B, E, C",
      "A, B, D, F, C, E"
    ],
    "correctIndex": 1,
    "explanation": "Synchronous code 'A' and 'F' execute first. The first then callbacks from each chain \u2014 'B' and 'D' \u2014 are registered in the microtask queue. 'B' executes and chains 'C' into the queue. 'D' executes and chains 'E' into the queue. The microtask queue continues draining: 'C', then 'E'. This is the interleaving pattern where two Promise chains alternate execution.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then"
  },
  {
    "id": "event-loop-041",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "hard",
    "question": "Why does the catch block fail to catch the error in the following code?",
    "code": "try {\n  setTimeout(() => {\n    throw new Error('async error');\n  }, 0);\n} catch (e) {\n  console.log('caught:', e.message);\n}",
    "options": [
      "throw does not work inside setTimeout",
      "setTimeout's 0ms delay causes it to execute before try-catch",
      "The setTimeout callback runs as a separate macrotask, and the original try-catch execution context has already exited by then",
      "Creating an Error object causes it to be automatically ignored"
    ],
    "correctIndex": 2,
    "explanation": "try-catch can only catch errors within its synchronous execution context. The setTimeout callback runs as a separate macrotask, and by the time it executes, the try-catch block's execution context has already been removed from the call stack. Errors in asynchronous callbacks must be handled inside the callback itself, or you should use Promises and async/await to propagate errors.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/try...catch"
  },
  {
    "id": "event-loop-042",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "medium",
    "question": "What is the correct order of phases in the Node.js event loop?",
    "options": [
      "poll \u2192 check \u2192 timers \u2192 close callbacks",
      "timers \u2192 close callbacks \u2192 poll \u2192 check",
      "check \u2192 timers \u2192 poll \u2192 close callbacks",
      "timers \u2192 pending callbacks \u2192 poll \u2192 check \u2192 close callbacks"
    ],
    "correctIndex": 3,
    "explanation": "The Node.js event loop (based on libuv) consists of several phases. The main order is: timers (setTimeout/setInterval callbacks) \u2192 pending callbacks (deferred I/O callbacks) \u2192 idle/prepare (internal use) \u2192 poll (wait for new I/O events) \u2192 check (setImmediate callbacks) \u2192 close callbacks (close events). Microtasks (nextTick, Promise) are processed between each phase.",
    "sourceUrl": "https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick"
  },
  {
    "id": "event-loop-043",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "easy",
    "question": "What is the output order of the following code?",
    "code": "setTimeout(() => console.log('A'), 100);\nsetTimeout(() => console.log('B'), 0);\nsetTimeout(() => console.log('C'), 50);\nconsole.log('D');",
    "options": [
      "D, A, C, B",
      "D, B, C, A",
      "D, B, A, C",
      "B, C, A, D"
    ],
    "correctIndex": 1,
    "explanation": "Synchronous code 'D' executes first. Then the three setTimeout callbacks are registered in the macrotask queue according to their delays. 'B' with 0ms delay enters the queue first, followed by 'C' at 50ms, and 'A' at 100ms. setTimeout's delay represents the minimum time before the callback is queued.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setTimeout"
  },
  {
    "id": "event-loop-044",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "hard",
    "question": "Which statement about AbortController and the event loop is correct?",
    "options": [
      "AbortController can interrupt synchronous code currently running on the call stack",
      "An abort() call is processed with higher priority than microtasks",
      "AbortController operates in a separate thread independent of the event loop",
      "Calling abort() synchronously fires registered abort event listeners, which can cancel in-progress async operations like fetch"
    ],
    "correctIndex": 3,
    "explanation": "When AbortController's abort() method is called, the AbortSignal's abort event listeners fire synchronously. APIs like fetch monitor the AbortSignal, and when it is aborted, they cancel the network request and reject with an AbortError. This does not interrupt synchronous code on the call stack \u2014 rather, it provides a mechanism to abandon waiting for async operation completion.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/AbortController"
  },
  {
    "id": "event-loop-045",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "Promise.resolve('ok')\n  .then(v => { console.log('A'); return v; })\n  .finally(() => console.log('B'))\n  .then(v => console.log('C:', v));\nPromise.reject('err')\n  .catch(e => { console.log('D'); return 'recovered'; })\n  .finally(() => console.log('E'))\n  .then(v => console.log('F:', v));",
    "options": [
      "A, D, B, E, C: ok, F: recovered",
      "A, B, C: ok, D, E, F: recovered",
      "A, D, B, C: ok, E, F: recovered",
      "D, A, E, B, F: recovered, C: ok"
    ],
    "correctIndex": 0,
    "explanation": "The two Promise chains interleave in the microtask queue. The first chain's then outputs 'A', and the second chain's catch outputs 'D'. Next, the finally callbacks execute: 'B' then 'E'. Since finally passes through the value without modifying it, the final then callbacks output 'C: ok' and 'F: recovered' in order.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/finally"
  },
  {
    "id": "event-loop-046",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "medium",
    "question": "Why does the race result not always resolve to 'timeout' in the following code?",
    "code": "function fetchWithTimeout(url, ms) {\n  return Promise.race([\n    fetch(url),\n    new Promise((_, reject) =>\n      setTimeout(() => reject('timeout'), ms)\n    )\n  ]);\n}\nfetchWithTimeout('/api/data', 5000);",
    "options": [
      "If fetch responds within 5 seconds, its result resolves first and wins the race",
      "Promise.race always returns the first argument",
      "setTimeout cannot guarantee exactly 5 seconds",
      "fetch executes synchronously"
    ],
    "correctIndex": 0,
    "explanation": "Promise.race returns the result of whichever Promise settles (fulfills or rejects) first. If fetch responds within 5 seconds, the fetch Promise resolves first and wins the race. If no response comes within 5 seconds, the setTimeout's reject('timeout') settles first, resulting in a timeout error. This is a pattern where two async operations compete in the event loop.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/race"
  },
  {
    "id": "event-loop-047",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "easy",
    "question": "What is the difference between recursive setTimeout and setInterval?",
    "code": "// Pattern A: Recursive setTimeout\nfunction repeat() {\n  doWork();\n  setTimeout(repeat, 100);\n}\n\n// Pattern B: setInterval\nsetInterval(doWork, 100);",
    "options": [
      "Patterns A and B work identically",
      "Recursive setTimeout starts the delay after the previous callback completes, ensuring stable intervals that account for callback execution time",
      "setInterval always guarantees more accurate intervals than recursive setTimeout",
      "Recursive setTimeout causes memory leaks"
    ],
    "correctIndex": 1,
    "explanation": "Recursive setTimeout starts the next delay only after the callback finishes executing. In contrast, setInterval registers callbacks at fixed intervals regardless of callback execution time. If doWork() takes 50ms, recursive setTimeout produces 150ms intervals (50ms execution + 100ms delay), while setInterval queues at 100ms intervals. Recursive setTimeout is safer when execution time may exceed the interval.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setTimeout"
  },
  {
    "id": "event-loop-048",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "console.log('A');\nPromise.reject('error')\n  .catch(() => console.log('B'))\n  .then(() => console.log('C'));\nPromise.resolve()\n  .then(() => { throw new Error('fail'); })\n  .catch(() => console.log('D'))\n  .then(() => console.log('E'));\nconsole.log('F');",
    "options": [
      "A, F, B, D, C, E",
      "A, F, B, C, D, E",
      "A, F, D, B, E, C",
      "A, F, B, D, E, C"
    ],
    "correctIndex": 1,
    "explanation": "Synchronous code 'A' and 'F' execute first. Then microtasks run: the first chain's catch (B) and the second chain's then (which throws) are both queued. B executes and enqueues its chained .then(C). The second chain's throw enqueues .catch(D). Now C and D are queued in that order \u2014 C runs first, then D. D resolves and enqueues .then(E). Finally E runs. Result: A, F, B, C, D, E.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/catch"
  },
  {
    "id": "event-loop-049",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "console.log('1');\nsetTimeout(() => console.log('2'), 0);\nrequestAnimationFrame(() => console.log('3'));\nPromise.resolve().then(() => console.log('4'));\nconsole.log('5');",
    "options": [
      "1, 5, 4, 2, 3",
      "1, 5, 4, 3, 2",
      "1, 5, 2, 3, 4",
      "1, 5, 3, 4, 2"
    ],
    "correctIndex": 1,
    "explanation": "Synchronous code runs first: '1', '5'. Then microtasks execute: Promise.then logs '4'. Next, before the browser repaints, requestAnimationFrame fires: '3'. Finally, the macrotask (setTimeout) runs: '2'. The key ordering is: synchronous > microtasks > requestAnimationFrame > macrotasks (setTimeout). rAF runs before the next paint but after microtasks.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/window/requestAnimationFrame"
  },
  {
    "id": "event-loop-050",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "medium",
    "question": "What is the purpose of queueMicrotask() in the event loop?",
    "options": [
      "It schedules a callback as a macrotask, similar to setTimeout(fn, 0)",
      "It runs the callback synchronously before continuing to the next line",
      "It schedules a callback in the microtask queue, which runs after the current task completes but before any macrotasks or rendering",
      "It creates a new thread to run the callback in parallel"
    ],
    "correctIndex": 2,
    "explanation": "queueMicrotask() schedules a function to run in the microtask queue. Microtasks are processed after the current synchronous task finishes but before the event loop picks up the next macrotask (setTimeout, setInterval) or performs rendering. This is the same queue used by Promise.then() callbacks. It's useful when you need to defer work without yielding to the browser's rendering pipeline.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-051",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output when button.click() is called programmatically?",
    "code": "button.addEventListener('click', () => {\n  Promise.resolve().then(() => console.log('micro1'));\n  console.log('listener1');\n});\nbutton.addEventListener('click', () => {\n  Promise.resolve().then(() => console.log('micro2'));\n  console.log('listener2');\n});\nbutton.click();",
    "options": [
      "listener1, micro1, listener2, micro2",
      "listener1, listener2",
      "micro1, listener1, micro2, listener2",
      "listener1, listener2, micro1, micro2"
    ],
    "correctIndex": 3,
    "explanation": "When button.click() is called programmatically (as opposed to a real user click), the event dispatch happens synchronously within the current call stack. Both listeners execute as part of the same synchronous call \u2014 'listener1' prints, then 'listener2' prints. The microtask checkpoint only occurs after the call stack is fully unwound, so both microtasks ('micro1', 'micro2') run after both listeners finish. With a real user click, the browser dispatches the event as a new task, and microtasks are flushed between each listener callback, producing: listener1, micro1, listener2, micro2. This distinction is important for understanding event dispatch behavior in tests vs real user interactions.",
    "sourceUrl": "https://javascript.info/microtask-queue"
  },
  {
    "id": "event-loop-052",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "medium",
    "question": "When are requestIdleCallback tasks executed relative to setTimeout and requestAnimationFrame?",
    "options": [
      "requestIdleCallback runs during idle periods after rendering and other tasks, making it the lowest priority among the three",
      "requestIdleCallback runs before requestAnimationFrame in every frame",
      "requestIdleCallback and setTimeout have the same priority",
      "requestIdleCallback runs immediately after microtasks, before any macrotasks"
    ],
    "correctIndex": 0,
    "explanation": "The browser event loop processes tasks in this general order within each frame: macrotasks (including setTimeout) \u2192 microtasks \u2192 rendering (including requestAnimationFrame) \u2192 idle callbacks (requestIdleCallback). Idle callbacks only execute when the browser determines there is remaining time in a frame after all higher-priority work is done. This makes requestIdleCallback ideal for non-urgent background work like analytics or pre-fetching, since it won't interfere with user interactions, animations, or critical rendering. If no idle time is available, the callback may be deferred indefinitely unless a timeout option is specified.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Window/requestIdleCallback"
  },
  {
    "id": "event-loop-053",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "easy",
    "question": "What happens to setTimeout and setInterval when a browser tab is in the background?",
    "options": [
      "They continue to execute at the same rate as when the tab is active",
      "They are completely paused and resume only when the tab becomes active again",
      "They are immediately cancelled by the browser",
      "The browser throttles them to a minimum interval (typically 1 second), reducing their execution frequency"
    ],
    "correctIndex": 3,
    "explanation": "Most browsers throttle timers in background tabs to conserve CPU and battery. Typically, setTimeout and setInterval callbacks in a background tab are clamped to a minimum interval of about 1000ms (1 second), regardless of the originally specified delay. This means a setInterval with a 100ms interval will only fire approximately once per second when the tab is not visible. requestAnimationFrame callbacks are usually paused entirely in background tabs. This throttling behavior is important to consider when building features that rely on precise timing, such as animations or real-time updates.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/setTimeout#reasons_for_delays_longer_than_specified"
  },
  {
    "id": "event-loop-054",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "medium",
    "question": "Why does the following async generator not yield values as expected?",
    "code": "async function* ticker(ms) {\n  while (true) {\n    await new Promise(r => setTimeout(r, ms));\n    yield Date.now();\n  }\n}\nasync function main() {\n  for await (const tick of ticker(100)) {\n    console.log(tick);\n    await new Promise(r => setTimeout(r, 500));\n  }\n}\nmain();",
    "options": [
      "async generators cannot be used with for-await-of loops",
      "The while(true) causes an infinite synchronous loop that blocks the event loop",
      "The generator yields timestamps approximately every 600ms instead of every 100ms because each iteration awaits both the generator and the 500ms delay sequentially",
      "Date.now() returns the same value because the event loop is paused during await"
    ],
    "correctIndex": 2,
    "explanation": "In a for-await-of loop, each iteration waits for the yielded promise to resolve, then executes the loop body. The generator awaits 100ms before yielding, then the loop body awaits 500ms. These delays are sequential, not concurrent \u2014 so each tick arrives roughly every 600ms, not every 100ms. The event loop is not blocked; it is free between awaits. To achieve true 100ms ticks regardless of processing time, you would need to decouple production from consumption using a queue or event-based approach.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for-await...of"
  },
  {
    "id": "event-loop-055",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "hard",
    "question": "What is the key difference between structuredClone and JSON.parse(JSON.stringify()) with respect to the event loop?",
    "options": [
      "JSON.parse(JSON.stringify()) is asynchronous while structuredClone is synchronous",
      "structuredClone uses a Web Worker internally, moving the copy operation off the main thread",
      "structuredClone handles circular references and non-JSON types (Date, RegExp, ArrayBuffer) while JSON serialization loses them, but both execute synchronously on the main thread",
      "structuredClone queues the copy as a microtask while JSON methods execute immediately"
    ],
    "correctIndex": 2,
    "explanation": "Both structuredClone and JSON.parse(JSON.stringify()) are synchronous operations that execute on the main thread and block the event loop during execution. The key difference is in what they can copy: structuredClone uses the structured clone algorithm which handles circular references, Date objects, RegExp, ArrayBuffer, Map, Set, and other non-JSON types. JSON serialization silently drops functions, converts Dates to strings, throws on circular references, and loses prototype information. Neither offloads work to another thread \u2014 for large data, both can cause frame drops.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/structuredClone"
  },
  {
    "id": "event-loop-056",
    "topic": "event-loop",
    "type": "debugging",
    "difficulty": "hard",
    "question": "Why does the 'unhandledrejection' event handler not prevent the error from appearing in the console in the following code?",
    "code": "window.addEventListener('unhandledrejection', (e) => {\n  console.log('caught');\n});\n\nPromise.reject(new Error('oops'));",
    "options": [
      "The handler must call e.preventDefault() to suppress the default browser behavior of logging the error",
      "The 'unhandledrejection' event only fires for rejections inside async functions",
      "Promise.reject() bypasses the event loop entirely and logs synchronously",
      "The handler must return false to suppress the error"
    ],
    "correctIndex": 0,
    "explanation": "The 'unhandledrejection' event fires when a Promise is rejected and no rejection handler is attached by the end of the microtask queue processing. However, simply listening for the event does not suppress the browser's default behavior of logging the error to the console. You must call event.preventDefault() inside the handler to tell the browser not to log it. This is similar to how DOM events work — the default action occurs unless explicitly prevented.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Window/unhandledrejection_event"
  },
  {
    "id": "event-loop-057",
    "topic": "event-loop",
    "type": "concept",
    "difficulty": "medium",
    "question": "How does window.postMessage() interact with the event loop when sending a message to the same window?",
    "options": [
      "The message handler executes synchronously, like dispatchEvent",
      "The message is queued as a microtask, executing before the next macrotask",
      "The message is queued as a macrotask, but typically with less minimum delay than setTimeout(fn, 0)",
      "The message bypasses both the microtask and macrotask queues"
    ],
    "correctIndex": 2,
    "explanation": "When you call window.postMessage() targeting the same window, the 'message' event is dispatched asynchronously as a macrotask. Unlike setTimeout which has a minimum delay (often 4ms for nested calls in browsers), postMessage typically has lower overhead, making it a common technique for scheduling immediate macrotasks with less delay. This is why libraries sometimes use a postMessage-based approach for breaking up long-running work into chunks without the setTimeout minimum delay penalty.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Window/postMessage"
  },
  {
    "id": "event-loop-058",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "medium",
    "question": "What is the output order of the following code?",
    "code": "const el = document.createElement('div');\nel.addEventListener('click', () => console.log('A'));\nel.addEventListener('click', () => console.log('B'));\n\nconsole.log('1');\nel.dispatchEvent(new Event('click'));\nconsole.log('2');",
    "options": [
      "1, 2, A, B",
      "1, A, 2, B",
      "1, A, B, 2",
      "A, B, 1, 2"
    ],
    "correctIndex": 2,
    "explanation": "When dispatchEvent() is called programmatically, event listeners execute synchronously within the dispatchEvent call — they are NOT queued as separate tasks. The call to dispatchEvent blocks until all listeners have completed. So '1' logs first, then dispatchEvent triggers both listeners synchronously ('A' then 'B'), and finally '2' logs. This is different from user-initiated events, where the browser creates a new task for each event dispatch.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/dispatchEvent"
  },
  {
    "id": "event-loop-059",
    "topic": "event-loop",
    "type": "output-prediction",
    "difficulty": "hard",
    "question": "What is the output order of the following code?",
    "code": "Promise.resolve().then(() => {\n  console.log('A');\n  Promise.resolve().then(() => console.log('B'));\n});\n\nPromise.resolve().then(() => {\n  console.log('C');\n  Promise.resolve().then(() => console.log('D'));\n});",
    "options": [
      "A, B, C, D",
      "A, C, B, D",
      "A, C, D, B",
      "C, A, D, B"
    ],
    "correctIndex": 1,
    "explanation": "Both top-level .then() callbacks are queued as microtasks. The first microtask runs and logs 'A', then schedules a new microtask for 'B'. The second microtask runs and logs 'C', then schedules a new microtask for 'D'. The microtask queue is drained in FIFO order: 'B' was scheduled first, then 'D', so 'B' runs before 'D'. The final order is A, C, B, D. This demonstrates that the microtask queue is processed sequentially — newly added microtasks go to the end of the queue and are processed after all currently queued microtasks.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask"
  },
  {
    "id": "event-loop-060",
    "topic": "event-loop",
    "type": "comparison",
    "difficulty": "easy",
    "question": "What is the difference between scheduler.postTask() and setTimeout() for task scheduling?",
    "options": [
      "They are identical — scheduler.postTask() is just an alias for setTimeout",
      "scheduler.postTask() executes callbacks as microtasks while setTimeout uses macrotasks",
      "scheduler.postTask() allows specifying priority levels (user-blocking, user-visible, background) while setTimeout only controls delay",
      "setTimeout can be cancelled but scheduler.postTask() cannot"
    ],
    "correctIndex": 2,
    "explanation": "The Scheduler API (scheduler.postTask()) provides priority-based task scheduling that setTimeout cannot offer. It accepts a priority option with three levels: 'user-blocking' (highest, for tasks critical to user interaction), 'user-visible' (default, for tasks the user will notice), and 'background' (lowest, for non-urgent work). This gives the browser better information to schedule tasks without blocking important user interactions. setTimeout only allows specifying a minimum delay, with no way to express priority. scheduler.postTask() also returns a Promise and supports AbortController for cancellation.",
    "sourceUrl": "https://developer.mozilla.org/en-US/docs/Web/API/Scheduler/postTask"
  }
]
